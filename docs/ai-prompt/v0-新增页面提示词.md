# Prism — v0 新增页面提示词

> 使用方式：按页面拆分，逐段复制到 v0 对话框中发送
> 覆盖范围：PRD 中 v0 原型未画出的页面（F8/F14/F15/F16/F18/F19/F20）
> 前提：基于现有项目代码，保持现有设计风格和组件库不变，所有文字使用中文

---

## 一、新增"模块关系图"Tab 页 /projects/[id]/relation-graph/page.tsx（F8）

在项目详情页新增一个 Tab "关系图"，位于"竞品对比"和"设置"之间。页面结构与现有 Tab 页保持一致（Header + Breadcrumb + Tab 导航 + 内容区）。

### 页面布局

1. 顶部区域（与全景图页面一致的 header + breadcrumb + tab 导航），Tab 导航改为 6 个：
```
全景图 | 产品线 | 需求工作台 | 竞品对比 | 关系图(当前高亮) | 设置
```

2. 内容区分为上下两部分：

**上方工具栏**（一行，左右分布）：
- 左侧：标题"模块关系图"+ 小字 Badge 显示"12 个节点 · 15 条关联"
- 右侧：3 个筛选 Toggle 按钮（用 Button variant="outline" size="sm"），默认全选中：
  - `depends_on 依赖` — 按钮旁一个蓝色实心小圆点
  - `related_to 相关` — 按钮旁一个灰色空心小圆点
  - `conflicts_with 冲突` — 按钮旁一个红色实心小圆点
- 最右侧：Button "添加关联"（variant="outline" size="sm"，Plus 图标）

**下方主区域**：一个 Card 占满剩余空间（min-h-[500px]），内部用 **纯 CSS/SVG 模拟** 一个力导向关系图（不用真的 React Flow，v0 原型用静态模拟即可）：

3. 关系图内容用如下方式模拟：
- 用 12 个圆角矩形节点（绝对定位在 Card 内），每个节点：
  - 样式：`rounded-lg border bg-card p-2 shadow-sm absolute cursor-pointer hover:border-primary/60`
  - 内容：完善度色块（小圆点）+ 节点名称（text-xs）
  - 鼠标 hover 时边框变 primary
- 用 SVG 画线连接节点：
  - depends_on：蓝色实线（stroke-blue-400 stroke-width-1.5）
  - related_to：灰色虚线（stroke-muted-foreground stroke-dasharray-4）
  - conflicts_with：红色实线（stroke-red-400 stroke-width-1.5）

4. 节点数据（模拟 AI 云平台项目的模块）：
```
节点数据（name, x%, y%, completion%）：
{ name: "创建推理服务", x: 20, y: 30, completion: 85 },
{ name: "自动扩缩容", x: 45, y: 20, completion: 70 },
{ name: "路由管理", x: 35, y: 55, completion: 60 },
{ name: "模型调度", x: 60, y: 40, completion: 45 },
{ name: "监控告警", x: 75, y: 25, completion: 50 },
{ name: "GPU管理", x: 15, y: 65, completion: 90 },
{ name: "配额管理", x: 50, y: 70, completion: 35 },
{ name: "用户管理", x: 80, y: 60, completion: 75 },
{ name: "存储管理", x: 25, y: 85, completion: 40 },
{ name: "镜像管理", x: 55, y: 85, completion: 55 },
{ name: "API网关", x: 70, y: 75, completion: 30 },
{ name: "计费统计", x: 85, y: 45, completion: 25 }

关联数据（from, to, type）：
{ from: "创建推理服务", to: "路由管理", type: "depends_on" },
{ from: "创建推理服务", to: "模型调度", type: "depends_on" },
{ from: "创建推理服务", to: "GPU管理", type: "depends_on" },
{ from: "自动扩缩容", to: "监控告警", type: "depends_on" },
{ from: "自动扩缩容", to: "配额管理", type: "depends_on" },
{ from: "路由管理", to: "API网关", type: "depends_on" },
{ from: "模型调度", to: "GPU管理", type: "depends_on" },
{ from: "监控告警", to: "计费统计", type: "related_to" },
{ from: "GPU管理", to: "存储管理", type: "related_to" },
{ from: "镜像管理", to: "存储管理", type: "related_to" },
{ from: "配额管理", to: "计费统计", type: "related_to" },
{ from: "用户管理", to: "配额管理", type: "related_to" },
{ from: "自动扩缩容", to: "路由管理", type: "conflicts_with" },
{ from: "GPU管理", to: "配额管理", type: "conflicts_with" },
{ from: "API网关", to: "模型调度", type: "related_to" }
```

5. 右侧或底部：点击某节点后显示侧边信息面板（用 Sheet 或 Card 模拟），包含：
- 节点名称 + 完善度进度条
- "关联列表"：列出该节点的所有关联（类型 Badge + 箭头方向 + 目标节点名）
- "查看档案" Button（链接到功能项档案页）

---

## 二、新增"行业动态"页面 /projects/[id]/insights/page.tsx（F14）

在 Tab 导航中新增"行业动态"（位于"关系图"和"设置"之间）。

### 页面布局

1. Header + Breadcrumb + Tab 导航（7 个 Tab）：
```
全景图 | 产品线 | 需求工作台 | 竞品对比 | 关系图 | 行业动态(当前高亮) | 设置
```

2. 内容区上方工具栏：
- 左侧：标题"行业动态" + Badge "8 条动态"
- 右侧：标签过滤下拉菜单（Select，选项：全部/技术趋势/产品发布/市场报告/融资收购）+ Button "添加动态"（Plus 图标）

3. 内容区主体：动态卡片列表（单列，间距 gap-4）。每张 Card：
- 顶部行（左右分布）：
  - 左：标签 Badge（如"技术趋势"用蓝色、"产品发布"用绿色、"融资收购"用紫色）+ 标题（font-medium）
  - 右：日期（text-xs text-muted-foreground）
- 中间：摘要文字（text-sm text-muted-foreground，2-3 行）
- 底部行（左右分布）：
  - 左：来源文字（text-xs，如"来源：36kr"）
  - 右：关联功能项的 Badge 组（如"创建推理服务""自动扩缩容"，variant="secondary" text-xs），点击跳转到档案页；如无关联则显示 Button "关联功能项"（variant="ghost" size="sm"，Link2 图标）

4. 模拟数据（8 条）：
```
[
  { title: "AWS SageMaker 发布多模型推理端点", tag: "产品发布", tagColor: "green", date: "2026-04-08", source: "AWS Blog", summary: "SageMaker 新增多模型推理端点功能，支持在单个端点上部署多个模型并自动路由请求，降低推理成本约 40%。", linkedFeatures: ["创建推理服务", "路由管理"] },
  { title: "阿里 PAI 推出 GPU 拼卡调度方案", tag: "技术趋势", tagColor: "blue", date: "2026-04-05", source: "阿里云公众号", summary: "PAI 发布基于 MIG 的 GPU 拼卡调度方案，支持 A100/H100 多实例切分，单卡最多 7 个独立实例。", linkedFeatures: ["GPU管理"] },
  { title: "Hugging Face 推出开源推理优化框架 TGI 2.0", tag: "技术趋势", tagColor: "blue", date: "2026-04-03", source: "Hugging Face Blog", summary: "TGI 2.0 支持推测解码、PagedAttention v2、动态批处理等特性，在 Llama 3 上吞吐量提升 3 倍。", linkedFeatures: ["模型调度"] },
  { title: "Replicate 完成 4000 万美元 B 轮融资", tag: "融资收购", tagColor: "purple", date: "2026-03-28", source: "36kr", summary: "模型即服务平台 Replicate 完成 B 轮融资，主打 Serverless GPU 推理，目前支持超 1 万个开源模型。", linkedFeatures: [] },
  { title: "Google Vertex AI 新增自动扩缩容策略配置", tag: "产品发布", tagColor: "green", date: "2026-03-25", source: "Google Cloud Blog", summary: "Vertex AI 推理端点支持自定义扩缩容策略，包括基于队列深度、GPU 利用率、自定义指标的多维度触发条件。", linkedFeatures: ["自动扩缩容", "监控告警"] },
  { title: "Nvidia Triton 推理服务器性能优化白皮书", tag: "技术趋势", tagColor: "blue", date: "2026-03-20", source: "Nvidia Developer", summary: "Nvidia 发布 Triton Inference Server 最佳实践白皮书，覆盖模型并发、动态批处理、模型预热等优化策略。", linkedFeatures: ["创建推理服务", "模型调度"] },
  { title: "MLOps 市场规模预测：2030 年达 450 亿美元", tag: "市场报告", tagColor: "orange", date: "2026-03-15", source: "Gartner", summary: "Gartner 最新报告预测全球 MLOps 市场将从 2026 年的 120 亿美元增长至 2030 年的 450 亿美元，CAGR 39%。", linkedFeatures: [] },
  { title: "字节 火山引擎 推出推理加速服务", tag: "产品发布", tagColor: "green", date: "2026-03-10", source: "火山引擎官网", summary: "火山引擎推出基于自研芯片的推理加速服务，针对大语言模型场景优化，首 token 延迟降低 60%。", linkedFeatures: ["创建推理服务"] }
]
```

---

## 三、新增"操作日志"页面 /projects/[id]/activity/page.tsx（F15）

不作为 Tab，而是作为项目设置的子页面，在项目设置页 Tab 栏新增"操作日志"Tab（位于"AI 配置"之后）。或者作为独立页面通过全景图"最近更新"卡片的"查看全部"链接进入。

### 页面布局

1. Header + Breadcrumb（我的项目 > AI云平台竞品分析 > 操作日志）+ Tab 导航（同项目详情页）。

2. 内容区上方工具栏：
- 左侧：标题"操作日志"+ Badge "128 条记录"
- 右侧 3 个筛选器：
  - 操作类型 Select（全部/创建/编辑/删除/导入）
  - 操作者 Select（全部/陈玥/王工/李工）
  - 时间范围 Select（全部/今天/近7天/近30天）

3. 主体：时间分组的操作流水列表。按日期分组（"今天""昨天""2026-04-08" 等），组内每条操作记录为一行（不用 Card，用 border-b 分隔的行）：

每行布局（flex items-center gap-3 py-3）：
- Avatar（h-7 w-7）显示操作者姓首字
- 操作描述（text-sm）：`{操作者} {操作动词} 了 {对象链接}`
  - 操作动词根据类型用不同颜色：创建=green，编辑=blue，删除=red，导入=purple
  - 对象是 Link，点击跳转到对应页面
- 右侧：时间戳（text-xs text-muted-foreground，如"14:32"）
- 最右侧：路径 Badge（text-xs variant="secondary"，如"推理服务 > 路由管理"）

4. 底部：分页器（上一页 / 页码 / 下一页）

5. 模拟数据：
```
今天（4条）：
- 陈玥 编辑 了 "创建推理服务" 的 功能描述 — 14:32 — 推理服务 > 创建推理服务
- 陈玥 添加 了 "路由管理" 的 竞品参考 — 13:15 — 推理服务 > 路由管理  
- 王工 编辑 了 "自动扩缩容" 的 技术实现 — 11:28 — 推理服务 > 自动扩缩容
- 陈玥 创建 了功能项 "拼卡管理" — 10:05 — GPU管理 > 拼卡管理

昨天（3条）：
- 李工 添加 了 "GPU管理" 的 工程经验 — 16:45 — 基础设施 > GPU管理
- 陈玥 导入 了 12 个功能项 — 15:00 — 批量操作
- 王工 删除 了 "临时测试模块" — 09:30 — 基础设施

2026-04-08（2条）：
- 陈玥 添加 了 "监控告警" 的 设计决策 — 17:20 — 运维支撑 > 监控告警
- 陈玥 创建 了项目 "AI云平台竞品分析" — 09:00 — 项目创建
```

---

## 四、档案页增加"AI 生成快照"按钮（F16）

修改现有的功能项档案页（openclaw/page.tsx 或对应的档案页），在版本时间线区块的标题栏右侧新增一个按钮。

### 修改内容

1. 在版本时间线区块（"版本演进"标题行）右侧新增条件按钮：
- 当版本记录 ≥ 3 条时：显示 Button "生成当前快照"（variant="outline" size="sm"，Sparkles 图标），可点击
- 当版本记录 < 3 条时：显示 disabled 状态按钮 + Tooltip "需要至少 3 条版本记录"

2. 点击"生成当前快照"后的交互模拟：
- 按钮变为 loading 状态（Loader2 旋转 + "生成中..."）
- 1.5 秒后在时间线上方展开一个 Card（border-primary/30 bg-primary/5），内容为：
  - 标题行："当前功能快照（AI 生成）" + Badge "待确认" variant="secondary"
  - 正文：2-3 段模拟的功能全貌描述文字（text-sm）
  - 底部操作栏：Button "保存快照"（variant="default"）+ Button "重新生成"（variant="outline"）+ Button "取消"（variant="ghost"）

3. 模拟的快照内容：
```
创建推理服务功能经历了 v1.6 到 v3.9.3 共 5 个版本的演进。

当前版本（v3.9.3）支持多模型部署、自动扩缩容和路由热更新。核心交互流程为：用户通过表单选择模型→配置资源规格→配置路由规则→提交创建。系统自动调度 GPU 资源并启动推理实例。

关键设计决策：采用声明式配置而非命令式操作（ADR-003），路由层使用独立网关解耦（ADR-005）。已知工程经验：大模型首次加载需要预热，冷启动时间约 30-60 秒。
```

---

## 五、搜索页增加语义搜索模式（F18）

修改现有搜索页 /search/page.tsx，在搜索框旁新增搜索模式切换。

### 修改内容

1. 在搜索框右侧新增搜索模式 Toggle 组（2 个按钮，用 div + rounded-md + bg-muted 包裹）：
- "关键词"：默认选中，高亮样式（bg-background shadow-sm）
- "语义"：未选中，text-muted-foreground

2. 当切换到"语义"模式时：
- 搜索框 placeholder 变为"输入自然语言描述，AI 搜索语义相关内容..."
- 搜索结果卡片新增一个字段：相关度分数（如"相关度 92%"），用 Badge variant="outline" 显示在结果右上角

3. 模拟语义搜索结果（输入"支付相关功能"时返回）：
```
[
  { title: "计费统计", project: "AI云平台", path: "运维支撑 > 计费统计", dimension: "功能描述", relevance: 92, snippet: "...平台**计费**模块负责统计各空间的资源使用量并生成**账单**，支持按小时/按天/按月三种**结算**周期..." },
  { title: "配额管理", project: "AI云平台", path: "基础设施 > 配额管理", dimension: "技术实现", relevance: 85, snippet: "...配额分配后触发**费用预估**计算，根据资源类型×使用时长估算**月度支出**..." },
  { title: "用户管理", project: "AI云平台", path: "平台管理 > 用户管理", dimension: "用户场景", relevance: 68, snippet: "...管理员需要查看各用户的**消费明细**和**充值记录**，支持导出**对账**报告..." }
]
```
注意：搜索词是"支付"，但结果中没有直接包含"支付"，而是匹配了"计费""结算""账单""费用""消费"等语义相关词。高亮显示这些语义匹配词（用 `<mark>` 或 font-medium text-primary）。

---

## 六、档案页/模块概览增加"导出"按钮（F19）

### 修改内容

1. 在模块概览页（/projects/[id]/modules/[moduleId]/page.tsx）的标题栏右侧新增 Button "导出报告"（variant="outline" size="sm"，Download 图标）。

2. 点击后弹出一个 Dialog：
- 标题："导出分析报告"
- 内容：
  - 导出范围 Checkbox 组（默认全选）：勾选要导出的维度（功能描述 ✓、用户场景 ✓、技术实现 ✓、设计决策 ✓、工程经验 ✓、测试分析 ✓、需求分析 ✓、竞品参考 ✓）
  - 导出格式 Radio 组：Markdown（默认选中）/ JSON
  - 包含子模块 Switch（默认开）
- 底部：Button "导出"（variant="default"）+ Button "取消"（variant="outline"）

3. 点击导出后模拟下载（显示 toast "报告已生成，正在下载..."）

---

## 七、新增"团队管理"页面 /teams/page.tsx + /teams/[id]/page.tsx（F20）

### 7.1 团队列表页 /teams/page.tsx

1. Header 与项目列表页一致，但左上角 Prism logo 旁新增导航切换：
- 两个 Tab 样式链接："项目"（Link to /projects）和"团队"（当前高亮，Link to /teams）
- 位于 logo 右侧，用 gap-6 的文字链接，当前页用 border-b-2 border-primary

2. 内容区：
- 标题行："我的团队"+ 右侧 Button "创建团队"（Plus 图标）
- 团队卡片列表（grid-cols-2 gap-4），每张 Card：
  - 左侧：团队头像（Avatar h-10 w-10，Fallback 为团队名首字）
  - 中间：
    - 团队名称（font-medium）+ 角色 Badge（"管理员"用蓝色 / "成员"用灰色）
    - 描述文字（text-sm text-muted-foreground，一行截断）
    - 底部：3 个小指标（text-xs text-muted-foreground）：N 个项目 · N 个成员 · 创建于 YYYY-MM-DD
  - 右侧：ChevronRight 图标

3. 模拟数据（3 个团队）：
```
[
  { id: "1", name: "AI 平台组", role: "管理员", description: "AI 云平台产品研发团队", projects: 2, members: 4, created: "2026-03-01", avatar: "AI" },
  { id: "2", name: "个人空间", role: "管理员", description: "个人项目默认团队", projects: 2, members: 1, created: "2026-02-15", avatar: "个" },
  { id: "3", name: "质量工程部", role: "成员", description: "质量工程与测试方法研究", projects: 3, members: 8, created: "2026-01-10", avatar: "质" }
]
```

### 7.2 团队详情/设置页 /teams/[id]/page.tsx

1. Header + Breadcrumb（团队 > AI 平台组）。

2. 页面分为左右布局：
- 左侧固定宽（w-[240px]）：垂直导航菜单，4 个选项（用 Button variant="ghost" 全宽对齐）：
  - 团队概览（默认高亮）
  - 成员管理
  - 项目管理  
  - 团队设置

3. 右侧主内容区（默认显示"团队概览"）：

**团队概览 Tab：**
- 顶部：团队名称（text-xl font-bold）+ 角色 Badge + 创建时间
- 3 个统计 Card 横排（grid-cols-3）：项目数 / 成员数 / 本月活跃天数
- 下方"团队项目"Card：项目列表表格（项目名 + 类型Badge + 完善度 + 最近更新），每行可点击

**成员管理 Tab：**
- 与现有项目设置的成员管理类似
- 表格列：Avatar + 用户名 + 邮箱 + 团队角色（团队管理员/团队成员）+ 项目数 + 操作
- 右上角 Button "邀请成员"
- 操作列：可修改角色、可移除（创建者不可移除）

**项目管理 Tab：**
- 表格列：项目名 + 类型Badge + 创建者 + 创建时间 + 操作
- 右上角两个 Button："新建项目" + "迁入已有项目"
- 点击"迁入已有项目"弹出 Dialog：
  - 标题："迁入项目到团队"
  - Select 下拉选择个人项目（显示未归属团队的项目列表）
  - 提示文字（text-sm text-muted-foreground）："迁入后，团队成员将可以访问该项目。原有权限保留。"
  - Button "确认迁入" + "取消"

**团队设置 Tab：**
- 团队名称输入框 + 团队描述输入框 + 保存按钮
- 分隔线
- 危险操作区域（border-destructive/30 bg-destructive/5 rounded-lg p-4）：
  - "解散团队" Button（variant="destructive"）
  - 警告文字："解散团队后，团队下的所有项目将变为个人项目。此操作不可撤销。"

---

## 八、更新全局导航

以上新页面需要修改项目详情页的 Tab 导航，将 5 个 Tab 扩展为 7 个：

```
全景图 | 产品线 | 需求工作台 | 竞品对比 | 关系图 | 行业动态 | 设置
```

同时在顶部 Header 的 Prism logo 旁新增全局导航切换（"项目" / "团队"），当用户从 /teams 路径进入时高亮"团队"。

所有新页面复用现有的 Header 组件模式（Prism logo + 搜索框 + 管理/设置/通知/用户头像/登出按钮），Breadcrumb 组件模式，以及 Card/Badge/Button/Avatar 等 shadcn/ui 组件。
