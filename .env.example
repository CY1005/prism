# Database
DATABASE_URL=postgres://prism:prism_dev_2026@127.0.0.1:5432/prism

# Auth (generate: openssl rand -base64 33)
AUTH_SECRET=

# API Key Encryption (generate: openssl rand -hex 32)
AI_KEY_ENCRYPTION_SECRET=

# Internal service token (generate: openssl rand -hex 16)
INTERNAL_TOKEN=
